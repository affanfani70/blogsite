<%- include('partials/header'); -%>
    <h1> Home </h1>
    <p>
        <%= Home %>
    </p>

    <% Post.forEach(element => {%>
        <h1><%= element.title %></h1>
        <p><%= element.content.substring(0, 60) %> ....  <a href="/post/<%= element._id %>">Read More</a></p> 
        <%  }); %>

         


<%- include('partials/footer'); -%>